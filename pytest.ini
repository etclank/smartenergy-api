# pytest.ini
[pytest]
asyncio_mode = auto
addopts = -q --disable-warnings
testpaths = tests
markers =
    postgres: requires a disposable PostgreSQL database
    celery: requires a disposable Redis broker/result backend and Celery worker
